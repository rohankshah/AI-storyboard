version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: storyboard-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: storyboard
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
